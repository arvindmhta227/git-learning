const button = "added a butoon";
console.log(button);

const login = "Login page added";
console.log(login);

const payment = "integerated the payment gateway";
console.log(payment);

const upi = "integerateed the upi also";
console.log(upi);